add chart modification
add chart transformation to stats view
add EWMA chart
add a license
have UI fetch individuals and CUSUM data separately
create a 'last n points' UI option
create a 'last t time' backend endpoint
add graceful handling of empty charts table
Have setup interval creation match zoom level
add autocorrelation mode
add list dataName cli option

add chart deletion
add first block point data reduction option
add "backwards" CUSUM and EWMA
dynamically fetch zoom window data on zoom in/out
add qqnorm plot
add mean and standard deviation to stats view